{
  "name": "developer-competency-platform",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter @dcvp/api --filter @dcvp/web dev",
    "dev:web": "pnpm --filter @dcvp/web dev",
    "dev:api": "pnpm --filter @dcvp/api dev",
    "db:generate": "pnpm --filter @dcvp/api prisma:generate",
    "db:migrate": "pnpm --filter @dcvp/api prisma:migrate",
    "db:seed": "pnpm --filter @dcvp/api db:seed",
    "build": "pnpm -r build",
    "typecheck": "pnpm --filter @dcvp/shared build && pnpm --filter @dcvp/api typecheck && pnpm --filter @dcvp/web typecheck",
    "lint": "pnpm --filter @dcvp/shared lint && pnpm --filter @dcvp/api lint && pnpm --filter @dcvp/web lint"
  },
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=20"
  }
}
